#include <iostream>
#include <vector>

class BinarySearcher {
public:
    int numTrees(int n) {
        std::vector<int> vec {};
        for (auto i = 0; i < n; i++) {
            vec.push_back(i);
        }

        
    }
};